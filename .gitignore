__pycache__/
*.pyc
.coverage

# Local secrets — never commit
.env
.env.*
!.env.example

# Nano Banana prompt library — fetched on demand by scripts/setup.js,
# not committed (large, refreshed twice daily upstream)
.claude/skills/nano-banana-pro-prompts-recommend-skill/references/*

# Impeccable design-hook runtime cache (config.json under .impeccable/ IS tracked)
.claude/skills/impeccable/scripts/.impeccable/
.impeccable/hook.cache.json
